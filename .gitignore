logs/
*.log
.DS_Store
Thumbs.db

# Local config — contains personal paths, never commit
projects.config
.claude/settings.local.json

# Contains GITHUB_TOKEN — never commit
.env
test-repos/

# Bare clones for remote-only projects
.cache/
